# Claude Code Controller — Cohort-Two New York Business-Date Repair

Run Claude Code on the Mac Mini from `/Users/jtsomwaru/projects/n8n-agent`, then paste everything below verbatim.

```text
You are the autonomous implementation controller for one bounded cohort-two defect: N03 uses the UTC calendar date for `run.as_of`, while the exclusion authority is generated by New York business date. Execution 1723 began at 2026-09-24T02:11:12Z (2026-09-23 22:11 America/New_York), requested a nonexistent 2026-09-24 exclusion file, and failed closed at N04. No state or downstream artifact changed.

Repository:
- repo: /Users/jtsomwaru/projects/n8n-agent
- remote: https://github.com/jsomwarux/n8n-agent.git
- required base: origin/main at 7863deee563406d96b6c508ad17777655d4bab57
- failure proof: /Users/jtsomwaru/.openclaw/workspace/proofs/2026-09-23/cohort-two-expanded-roster-pilot-1723/receipt.md
- handoff: /Users/jtsomwaru/.openclaw/workspace/memory/job-state/claude-business-date-repair.json

Before editing:
1. Read CLAUDE.md and tasks/lessons.md completely.
2. Fetch origin and verify origin/main is exactly the required base. Stop BLOCKED if it differs.
3. Verify `gh repo view jsomwarux/n8n-agent --json visibility,isPrivate` reports PRIVATE.
4. Create an isolated worktree at `/Users/jtsomwaru/.openclaw/workspace/.worktrees/n8n-cohort-two-business-date` on branch `eve/cohort-two-business-date` from exact origin/main. Do not modify the user's main checkout.
5. Reproduce the defect in an automated test before implementation: at `2026-09-24T02:11:12Z`, N03 must select business date `2026-09-23` for `America/New_York`, not UTC date `2026-09-24`.

Task:
Implement the smallest durable New York business-date repair. Do not redesign discovery, roster logic, pagination, ranking, gates, model prompts, state schemas, or downstream authority.

Required contract:
1. `started_at` remains an exact UTC ISO timestamp.
2. `run.as_of` is the calendar date in the workflow's business timezone, `America/New_York`.
3. `run.contact_universe_path` interpolates that same business date.
4. N06 continues to require the exclusion report's `generated_at` date to equal `run.as_of`; do not loosen or bypass this fail-closed gate.
5. Use a DST-safe IANA-timezone calculation. Do not hardcode a UTC offset and do not derive the date by subtracting a fixed number of hours.
6. Keep one authoritative business-timezone value and prove it matches the workflow setting. Prefer an explicit config field validated fail-closed by N03 and asserted equal to `workflow.settings.timezone`; if a simpler implementation is chosen, it must still have a regression guard preventing the N03 timezone and workflow timezone from drifting.
7. Reject missing, malformed, or unsupported business-timezone configuration before any file fetch or model call.
8. Preserve the existing behavior at ordinary daytime timestamps.
9. Update stale UTC-day comments in the test harness/docs so they state the actual business-date contract.
10. Update tasks/lessons.md with the reproduced operational lesson: UTC midnight can cross before the New York business day, so every date-keyed producer/consumer pair must share one explicit IANA timezone.

TDD and verification:
1. Add failing tests before implementation.
2. Cover at minimum:
   - 2026-09-24T02:11:12Z -> 2026-09-23 in America/New_York;
   - 2026-09-24T04:00:00Z -> 2026-09-24 in America/New_York;
   - a winter EST boundary and a summer EDT boundary;
   - invalid/missing timezone fails before N04/public/model work;
   - `started_at` stays UTC while `as_of` is New York date;
   - contact-universe path uses the same `as_of`;
   - source/render equality and workflow timezone equality.
3. Update authored sources first and regenerate rendered nodes/workflow JSON through checked-in generators. Never hand-edit generated artifacts.
4. Run focused tests during development.
5. At the apparent final SHA run the full bootstrapped suite with zero failures and zero skips, regeneration/equality, topology, inactive/no-send guards, credential/PII scan, repository visibility, and clean worktree.
6. Launch one fresh non-builder review in a clean clone at the exact SHA. The reviewer must attack UTC-midnight boundaries, DST transitions, invalid timezones, config/workflow timezone drift, stale exclusion acceptance, generated-file drift, activation/send boundaries, and non-empty test corpora.
7. If FAIL, repair only reproduced defects test-first and repeat with one fresh reviewer. Maximum two repair cycles. A new architecture decision or third failure stops HUMAN_DECISION/BLOCKED.
8. On CONFIRM, push the exact confirmed SHA, open/update a PR against main, and wait for available checks. Do not merge or deploy.

Prohibited:
- touching live n8n, live cohort state, credentials, secrets, OpenClaw config, or Mission Control;
- public prospect/model calls, a pilot run, real outreach, drafting, sends, activation, schedules, heartbeat, or suppression capability;
- weakening the same-day exclusion gate;
- modifying installed n8n;
- merging the PR.

Handoff JSON must include:
- status: COMPLETE | BLOCKED | HUMAN_DECISION | BLOCKED_NOTIFICATION;
- repo, base SHA, branch, final SHA, PR URL;
- exact files changed;
- focused/full/zero-skip/generation/topology/security/visibility results;
- fresh reviewer verdict and reviewed SHA;
- proof workflows remain inactive/send-free;
- blockers, next_owner, exact_next_action;
- notification_attempts with the exact acknowledgment JSON or error for every attempt.

Write the handoff to:
/Users/jtsomwaru/.openclaw/workspace/memory/job-state/claude-business-date-repair.json

For EVERY terminal status, notify Eve after writing the handoff:
openclaw system event --session-key agent:main:telegram:direct:6608544825 --text "Claude lane cohort-two-business-date: <STATUS>; handoff at /Users/jtsomwaru/.openclaw/workspace/memory/job-state/claude-business-date-repair.json" --mode now --expect-final --json

Persist the returned JSON in `notification_attempts`. Require acknowledged/final delivery. Retry exactly once if not acknowledged. If the second event attempt is not acknowledged, send one direct fallback to JT's Telegram using the approved local OpenClaw messaging surface, record the receipt, set `BLOCKED_NOTIFICATION` only if both routes fail, and stop. Never claim Eve was notified without a stored receipt.

Terminal response: one line only — status + handoff path.
```

# build-pm-triage-intake-to-dispatch
# Pass 3: Build Showcase
# build: PM Maintenance Triage Template (n8n)
# platform: X | banked: true | posted: false

tenant submits a maintenance request.

claude classifies urgency and category. vendor gets routed and notified. tenant gets an acknowledgment. if the vendor goes quiet, the system escalates before the coordinator ever pulls it up.

the coordinator's inbox sees exceptions only.

configurable per property in about 2 hours. same logic, different inputs.

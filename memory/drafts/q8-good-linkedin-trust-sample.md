Built the workflow around the approval step because that is where the business risk sits.

The scraper was the easy part.

The useful part was making sure the output landed in the spreadsheet the client already checks, with sensitive rows held in an approval queue and a manager assigned as the owner before any follow-up left the system.

That is the difference between a workflow that demos well and one a real operator can trust.

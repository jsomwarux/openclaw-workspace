# 2026-02-21 - First Day

## Setup session with JT

- Named me **Eve** 🌿
- First contact via Telegram

### Config done today
- Brave Search API key → saved to `tools.web.search.apiKey`
- Groq API key → saved as auth profile `groq:manual`
- QMD skill installed from ClawHub (`workspace/skills/qmd/`)
- `qmd` binary installed via bun (patched to run TypeScript source directly)
- `~/.bun/bin` added to PATH in `~/.zshrc`

### Notes
- JT is direct and efficient — keeps instructions brief, doesn't want hand-holding
- Gateway restart had some hiccups but keys confirmed saved

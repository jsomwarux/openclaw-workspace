# Prompt for Fable: Final AI Workflow Growth Operating System Synthesis

Attach `2026-09-09-ai-workflow-growth-operating-system-v2.md` to the same Fable conversation that contains your earlier analysis and the Grok proposal. Then paste everything below as the message.

---

You are producing the final implementation plan for JT Somwaru's AI Workflow Growth Operating System.

## Your role

Act as a principal systems architect and operator. Synthesize the strongest parts of:

1. your prior Fable analysis in this conversation;
2. Grok 4.6's proposal already provided in this conversation; and
3. the attached `2026-09-09-ai-workflow-growth-operating-system-v2.md`.

The attached v2 is the base document and current source of truth for the synthesis. Improve it where evidence supports a change. Do not replace it with a greenfield architecture, summarize it, or write another critique of the three plans.

## Current objective

Produce one complete, standalone, implementation-ready Markdown plan that JT can hand to builders without needing the earlier conversation, the Grok response, or the attached v2.

The system must turn JT's strongest capability, building controlled automated workflows with n8n and selectively adding LLM reasoning, into:

1. qualified consulting prospect research and outreach packets;
2. proof-led LinkedIn and X content with one strong image per post;
3. verified job discovery plus tailored resume and cover-letter packages;
4. one configurable marketing engine for Yardstick, Nash Satoshi, and Action Arena;
5. a low-friction daily review experience in the existing Mission Control.

## Authoritative current-state corrections

Treat these as binding unless the attached v2 contains newer verified evidence:

- The old 90 Day Playbook and its freeze list were retired on September 8, 2026. Do not revive them or use their September gates as current authority.
- Do not make the old September 15 Altmark/Yair deadline an unconditional action. Current correspondence must be verified before any client follow-up is proposed. No agent sends it.
- MSI is fully paid and closed, with the laptop returned and no current follow-on engagement.
- Karen is not a property-operator referral source. Do not include that ask.
- The old 57 prospect folders are not 57 fresh send-ready contacts. Current preflight evidence says they are already active/pitched, previously sent, or otherwise suppressed. Any prospect must be requalified against named buyer, reachable channel, freshness, proof match, and prior-contact state.
- Watchdog and any unlaunched delinquency workflow are not default proof and must not be described as live unless current evidence proves that exact claim. Prefer permission-safe paid, accepted, or shipped work.
- `jt-ops` is the shared spine. Do not create a second generic `workflow-growth-engine` repository.
- The current `jt-ops` problem is operational, not hypothetical: the latest verified run validated 653 records and committed 30 postings, then failed with 600 staged bodies because ingestion did not drain the queue. Branch visibility, receipt-name drift, backpressure, extraction, and CI must be repaired before new recurring lanes depend on it.
- Mission Control/Convex is the only human task and approval queue. Do not create a Notion or ClickUp duplicate. Notion remains a content calendar/swipe-file surface only.
- OpenClaw/Eve remains the orchestrator, memory layer, Telegram interface, policy enforcer, and independent-verification router. It is not the sole execution truth and may not grade its own work. Durable execution truth lives in domain artifacts, receipts, PRs, and owning systems.
- `/Users/jtsomwaru/projects/n8n-agent/tasks/lessons.md` remains the canonical n8n implementation knowledge. Do not copy it into a second editable skill. Other systems may use versioned, hashed adapters or references.
- JT performs every send, post, job application, purchase, RSVP, domain/DNS change, schedule activation, and production deployment unless he separately approves an exact bounded action.
- AgentGuard never appears in resume Key Projects.

## Settled architecture decisions

Preserve these unless you identify a specific contradiction and repair it explicitly:

1. Extend `jt-ops`; do not build a second spine.
2. Keep domain execution in existing owner systems: n8n for workflow runtime, the existing job-market agent for jobs, app repositories for product truth, Google Drive for final job documents, and Mission Control for human decisions.
3. Use three systems plus one board:
   - Research and Match Engine for prospects and jobs;
   - Content Engine for JT LinkedIn, JT X, Yardstick, Nash Satoshi, and Action Arena;
   - Outreach Delivery as a separately gated sending layer;
   - existing Mission Control as the only review board.
4. Start manual buyer motion immediately while infrastructure is repaired. Automation catches up to proven work; it does not block it.
5. Requalify a new/reactivation cohort of five prospects. Do not treat the old 57 as the cohort.
6. Outreach intelligence and outreach delivery remain separate. No sending fleet is approved merely because packet generation works.
7. Warmup may be prepared in parallel only after five packets pass source/copy QA and JT separately approves current spend, domains, inboxes, DNS, ownership, and shutdown procedure. Warmup approval does not authorize campaigns.
8. Use GPT as the initial LinkedIn writer and Grok as the initial X writer/researcher, then decide future routing using blind JT ratings, edit distance, and factual accuracy.
9. Grok Automations is an optional scheduled X/news research worker. Grok Bot is an optional bounded browser/computer worker that must prove incremental value. Grok Build, Codex, and Claude Code are builders, selected by verified output quality.
10. Use one strong image per content item. No carousels. Workflow screenshots require privacy and legibility checks; source screenshots require visible attribution and appropriate usage; other posts receive a strong Claude Design prompt or equivalent visual brief.
11. Harden the existing Job Hedge. Generate packages only for officially verified live roles that clear the evidence-backed competitiveness gate. Never use a daily package quota.
12. App marketing uses one configurable chassis. A lane activates only when the product, value proposition, claims, screenshots, and conversion destination are verified live.
13. Events are at most a lightweight monthly query. Passive-income discovery remains parked until proprietary pain, workflow, and outcome data can make it non-generic.
14. Feedback is appended as raw evidence. Weekly improvement produces proposed diffs with evidence and regression tests. No agent rewrites its own instructions automatically.
15. Normal JT review time is 30 to 45 minutes daily. Ninety minutes is an overflow alarm, not a target.

## Required improvements to the attached v2

Read every line before revising. Then:

1. Remove stale or speculative current-state assertions that are not necessary for implementation. Preserve dated evidence as evidence, not permanent logic.
2. Resolve any remaining circular approval gates, split owner surfaces, or places where a registry says paused but no named actor can pause the real schedule.
3. Make the boundary between `jt-ops`, n8n source files, deployed n8n workflow state, OpenClaw, Mission Control, job-market files, app repos, Notion, Drive, and any sequencer exact.
4. Define the smallest viable schemas/contracts and name their exact files and writers. Avoid rebuilding mature functionality that already exists in `jt-ops`.
5. Preserve the seven-field human card UX: title, why, exact steps, paste-ready prompt if needed, where to paste it, done condition, and feedback box. Add machine fields only where required for provenance, approval, idempotency, and routing.
6. Keep manual output parallel to repairs. Do not delay qualified outreach or publish-safe content until a multiweek platform build completes.
7. Keep all paid/external/account-changing actions behind explicit, exact approval gates. Do not instruct JT to buy domains, open inboxes, change DNS, archive tasks in bulk, send messages, or activate schedules as though the plan itself were authorization.
8. Define deterministic tests for freshness, source citation, verified-versus-inferred labeling, permission-safe proof, duplicate/suppression state, approval-hash invalidation, run receipts, branch visibility, delivery, and external outcomes.
9. Define failure behavior and rollback for partial runs, stalled queues, unavailable models, failed delivery, revoked OAuth, stale sources, and unconsumed packets.
10. Define the daily and Friday operating loops, a global WIP cap, lane-specific success metrics, cost gates, promotion gates, and kill rules.
11. Keep the implementation practical. Prefer repairing and adapting working surfaces over adding abstractions, agents, databases, or platforms.
12. If a current platform capability or price is material and may have changed, verify it from an official primary source. Cite it near the claim. If you cannot verify it, label it `UNVERIFIED` and turn it into a pre-activation check rather than an assumption.

## Required output

Return exactly one standalone Markdown document titled:

`# AI Workflow Growth Operating System v3 — Final Implementation Plan`

The document must include, in this order:

1. Executive decision and operating thesis.
2. What is in scope, deferred, and explicitly excluded.
3. Current-state facts versus assumptions requiring verification.
4. Final architecture and one-owner-per-fact table.
5. Exact tool/model routing, including Grok app, Grok Automations, Grok Bot, xAI API, GPT, OpenClaw, n8n, GitHub Actions, Claude Code/Codex/Grok Build, Mission Control, Notion, Drive, X API, enrichment, and the deferred sequencer.
6. Shared contracts with exact filenames, required fields, writer, readers, validation, and source owner.
7. Mission Control card contract and daily/weekly human loop.
8. Immediate manual track that can begin without new infrastructure.
9. `jt-ops` repair track with red-to-green acceptance tests.
10. Prospect research and AI-recipe/outreach packet system.
11. Outreach-delivery readiness and gated scale path.
12. LinkedIn/X content and one-image system.
13. Existing Job Hedge hardening and package-generation system.
14. App-marketing chassis and product-readiness gates.
15. Optional/deferred lanes.
16. Security, privacy, credentials, platform-policy, and approval controls.
17. Observability, run receipts, consumer contracts, and failure recovery.
18. Costs and external purchase decision gates.
19. Phased implementation sequence with dependencies, parallel tracks, exact acceptance criteria, and global WIP limits.
20. Scoreboards, promotion gates, pause/kill rules, and 30-day decision review.
21. First bounded implementation checkpoint: exact authorized work and exact prohibited work.
22. Final activation checklist.
23. One recommended immediate next action.

## Writing and quality rules

- Produce the final plan, not an audit, preamble, options memo, or explanation of how you synthesized it.
- Make it standalone. Do not say “as Eve said,” “as Grok said,” “in the attached file,” or rely on earlier chat context.
- Use direct language and have a clear recommendation.
- Use checkbox tasks for implementation work.
- Every multi-step build task must name the owner, authoritative files/surfaces, prerequisites, writes, prohibited writes, verification commands or observable checks, pass criteria, failure behavior, and approval boundary.
- Separate infrastructure success, task success, delivery success, human action, and commercial/audience outcome.
- Do not fabricate file paths, APIs, product capabilities, prices, current customer state, proof, or command output.
- Do not reintroduce retired mandates, stale deadlines, generic passive-income ideation, a duplicate task board, or a duplicate shared repository.
- Do not authorize execution. This output is a plan only.
- Do not optimize for length. Optimize for completeness without repetition.

Before finalizing, run this internal review and fix every failure:

1. Is there exactly one authoritative owner for every fact and external outcome?
2. Can any agent send, post, apply, purchase, RSVP, deploy, alter DNS, or activate a schedule without exact approval? If yes, fix it.
3. Does any manual buyer/content action wait unnecessarily for infrastructure? If yes, fix it.
4. Does any automated lane start before its source truth, verifier, consumer, cost ceiling, and real disable procedure are proven? If yes, fix it.
5. Could the plan create duplicate Mission Control, n8n lessons, prospect, job, app, proof, or content truth? If yes, fix it.
6. Are the old 57, Watchdog, Altmark deadlines, retired 90-day mandate, or stale client assumptions used as current truth? If yes, fix it.
7. Can JT operate the system in 30 to 45 minutes on a normal day? If no, reduce WIP and output volume.
8. Does each lane have a measurable promotion gate and a real kill rule? If no, add them.
9. Could a fresh builder execute each phase without inventing ownership or architecture? If no, add only the missing detail.
10. Is any paragraph commentary that does not change implementation behavior? If yes, cut it.

Return only the final Markdown plan.

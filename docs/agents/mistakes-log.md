# Mistakes Log (full archive)
> Source of truth for all past mistakes. AGENTS.md keeps only the last 3 entries inline.
> Every entry MUST have: (1) specific failure, (2) root cause one level deeper than "I forgot," (3) concrete rule that prevents recurrence.

| Date | Mistake | Fix |
| 2026-03-19 | LinkedIn DMs drafted with wrong CTA (20-min call ask) and "I built X" in M1 despite both being explicitly banned in cold-email/SKILL.md. Root cause: drafted from session memory instead of loading the skill file. Same root cause as the 2026-03-19 em dash violation. | Rule: **cold-email/SKILL.md must be loaded and the rules checklist run against every draft before output is generated. Memory of skill rules does not substitute for reading the file.** |
| 2026-03-19 | Added pre-output checklist to cold-email skill, then uploaded the existing draft without running the checklist against it. Binary-close CTA ("Is that a conversation worth having?") survived because the checklist was written but not applied. | Rule: **The checklist must be shown inline in the reply with each item marked ✅/❌ before any upload happens. Writing the checklist and running the checklist are not the same action.** |
| 2026-03-19 | LinkedIn InMail drafts missing subject lines entirely. InMails require subjects — this is a basic requirement of the format. Root cause: drafted without loading cold-email/SKILL.md, which specifies subject line rules explicitly. | Rule: **Every InMail draft must include a subject line. Check: subject present, 2-4 words, lowercase, internal-looking. No subject = draft is incomplete.** |
| 2026-03-19 | Em dash persisted in revised LinkedIn DM body after explicit correction pass. Root cause: grep check was run but pattern excluded lines starting with message text, so the Joshua M1 body line was missed. | Rule: **After any em dash removal pass, grep raw message body text directly and read it. Never trust a pattern-exclusion grep as the final check.** |
| 2026-04-02 | Resume and cover letter generated with em dashes throughout despite the job-application skill explicitly banning them. JT had to flag it. Root cause: wrote the files directly without loading job-application/SKILL.md first. Skill file contains the em dash rule clearly — it would have been caught immediately if read. | Rule: **Job application packages must load job-application/SKILL.md before writing a single word. Reading the skill and following the skill are not the same action. Em dashes are an ATS/AI-tell violation listed in the skill. No skill read = guaranteed rule miss.** |
| 2026-03-18 | Resume update delivered as an uploaded markdown file (.md) instead of a formatted .docx. Job application skill explicitly says "NEVER write resume as markdown — always generate .docx using build_resume_docx.py." JT had to flag it. | Root cause: updated the .md draft file first out of habit, then uploaded that instead of running the build script. Rule: **Job application skill = read it and follow Step 4 immediately. The output is always .docx via build_resume_docx.py. Updating the .md is optional reference only — never the deliverable. If I'm uploading a .md to Drive, I'm doing it wrong.** |
| 2026-03-17 (3) | Ran 9 sequential exec calls debugging the task board (assignee casing, project name, schema checks) without a single interim reply. Gateway froze, JT had to manually restart. | Root cause: treated "debugging a tool call failure" as a single atomic operation, not a multi-step chain. Rule: any exec loop >2 calls without resolution = send status update first ("Board is returning 500, investigating..."). Hard cap of 4 tool calls between replies applies even during debugging. |
| 2026-03-17 (3) | Resume research + writing chain (6 web searches/fetches + 3 large file writes = ~10 sequential tool calls, ~3 min silent) caused gateway to freeze and JT had to manually restart. No interim reply was sent during the chain despite Rule 9 requiring one for >2 tool calls / >15s. | Root cause: treated a multi-step research+writing task as one atomic operation instead of breaking it into stages. Rule: **Any task with research phase + writing phase MUST send a "Research complete, writing now" interim reply between phases. Never chain research → writing without a mid-task status message. Target: ≤4 tool calls between replies in active conversation.** |
| 2026-03-17 (2) | Outreach draft for Henick-Lane contained a fabricated contact: "Gregg Rothman, Owner/COO" with an invalid LinkedIn URL. Real contact is Thomas (Tom) Cahill, President (linkedin.com/in/thomas-cahill-07b00b86). JT caught it when the LinkedIn URL didn't resolve. | Root cause: outreach pipeline agent invented a plausible-sounding name and LinkedIn URL instead of only using confirmed contacts. Rule: **LinkedIn profile URL must resolve to a real person at that company before it goes in any draft. If no confirmed profile is found via search, the draft must be flagged as "no confirmed LinkedIn contact — manual search needed" and NOT sent with a fabricated URL.** |
| 2026-03-17 | Called `cron list` (all 35 jobs + full payloads) to check status of ONE cron. Filled context window, caused overflow errors on every subsequent message including "are you there?" — forced JT to manually restart gateway. | Root cause: defaulted to the broadest tool call instead of the narrowest one. Rule: **To check a single cron's status or run history, ALWAYS use `cron runs --jobId [id]` or check the specific job only. NEVER call `cron list` in an active conversation — it loads 35 full payloads and will overflow context.** |
|------|---------|-----|
| 2026-03-15 | Gateway was silently dead for ~13 hours (10 PM Sat – 10:55 AM Sun). JT didn't receive his morning brief. Root cause 1: `context-mode` Claude plugin (PID 3682) had been running since Wed 1PM — 5 days, 100% CPU, 2.6 GB RAM — causing OOM pressure until macOS killed the gateway. Root cause 2: Spanish lesson cron was trying to deliver to `@jtsomwaru` (username that doesn't resolve) instead of `6608544825` — delivery failures on startup contributed to crash loop. Root cause 3: Missed the runaway plugin because there was no memory monitoring. | Fixes: (1) Watchdog LaunchAgent (`com.openclaw.gateway-watchdog`) installed — runs every 10 min, kills context-mode if RSS >1.5GB, kicks gateway if dead. (2) Spanish lesson delivery updated to `announce, channel: telegram, to: 6608544825`. Rule: any isolated cron delivering to JT must use numeric Telegram ID `6608544825`, never a username. Rule: check for runaway processes when diagnosing gateway crashes — `ps aux \| sort -k4 -rn \| head -10` is the first diagnostic step. |
| 2026-03-13 | Morning brief surfaced a 3-week-old content draft ("Amodei 50% jobs") in the One Concrete Action section — JT looked for it in Drive, it didn't exist as a current item, wasted his time. Root cause: tasks.md had a Feb 24 entry that was never cleared; morning brief read it without checking the `Updated:` date. | Rule: Before surfacing ANY task in the morning brief (especially One Concrete Action), check the `Updated:` date. If > 7 days old and it references a specific content draft, job application, or Drive link — skip it and clear it. Stale tasks create false urgency. Added explicit staleness check to HEARTBEAT.md morning brief instructions. |
| 2026-03-13 | Friday LinkedIn post included an invented dollar figure ("$500K+ deployments") and implicitly criticized Spectrum Enterprise — both unverifiable and inappropriate. | Root cause: invented a personal anecdote to make the post feel specific. Rule: Never fabricate specifics (dollar amounts, outcomes, timelines) JT hasn't confirmed. Never frame former employers negatively. Hard Rules section added to content-voice.md. |
| 2026-03-12 | Repeated pattern: said "fixing that now" / "doing X now" then went silent after tool calls completed — JT had to follow up asking "complete?" multiple times in the same session. | Root cause: treated the tool execution as the end of the task. Forgot that JT sees nothing until a reply is sent — "doing it" and "telling JT it's done" are two separate required steps. Rule: every task that starts with a "doing X now" message MUST end with an explicit completion confirmation. The sequence is: (1) brief status reply, (2) do the work, (3) send a clear "Done" or result summary. Step 3 is not optional. |
| 2026-03-11 | MEMORY.md was stale across multiple sections (Agentforce overnight ban still listed as active after being lifted, Cowork service listed as active after being closed, Avallon listed as active after being closed, H.C. Oswald listed as "ready to send" after outreach was sent, jtsomwaru.com entry 2 weeks out of date). JT had to manually identify and request fixes. | Root cause: no mandatory rule tied MEMORY.md updates to the moment decisions were made. Rule #1 ("no mental notes") exists but wasn't specific enough to trigger MEMORY.md edits at decision time. Rule: MEMORY.md Live-Update Rule added above — any decision, status change, or strategic shift must update MEMORY.md in the same turn before replying. "I'll update it later" = Rule 1 violation. |
| 2026-03-11 | Overnight agent re-added B2B Account Service Agent card to jtsomwaru.com — THIRD offense (prior: 2026-03-07, 2026-03-09). Rule exists in both overnight/AGENT.md AND portfolio-updater/AGENT.md. | Root cause: the coding sub-agent reasoned "graphic B2BAccountAgentGraphic exists in project-graphics/index.tsx → must be a missing card → add it." The sub-agent treated graphic existence as a signal to add the card, bypassing the written exclusion. Rule added to portfolio-updater/AGENT.md: "A graphic component exists — it is an INTENTIONAL ORPHAN. Its existence ≠ the card should be added. Do not import it, map it, or reference it. Hard stop." |
| 2026-03-11 | Gateway froze twice — JT had to manually restart both times. Cause: ran `claude --print` and `claude [args]` subprocesses from exec inside the main session to try to install a Claude Code plugin. These block synchronously and freeze the gateway. | Root cause: conflated "exec can run any shell command" with "exec is safe for long-running blocking subprocesses." Rule 9a: NEVER run claude CLI from exec in main session. Plugin slash commands require JT to run them in a terminal. Use sessions_spawn runtime=acp for any Claude Code delegation. |
| 2026-03-11 | JT had to tell Eve to add Claude Code/OpenClaw technical content to the content system. The viral swipe cron had been collecting high-performing Claude Code and AI builder posts for weeks. The skills researcher was flagging Claude Code discussion. The data was there — but no system closed the loop. | Root cause: intelligence systems (swipe cron, skills researcher) are designed to collect and filter, but there's no synthesis step that compares what's performing well externally against what JT is currently producing and identifies the gap. Rule: weekly-synthesis cron must include a Content Strategy Gap Analysis step. Viral swipe cron must flag both FORMAT patterns (3+ appearances) AND TOPIC patterns (1000+ combined engagement). |
| 2026-03-11 | Heartbeat fired mid active conversation → ran full cron audit (30-job JSON + multiple sequential updates) without sending a status reply → session froze → JT had to manually restart gateway. | Root cause: "Reply before chaining tools" rule exists but had no specific heartbeat carve-out. Rule added to AGENTS.md rule #10: heartbeat during active conversation = HEARTBEAT_OK immediately, no exceptions. Full protocol is for idle sessions only. |
| 2026-03-11 | JT had to manually restart gateway mid-session. Caused by a 7+ tool chain (web searches → web fetches → file writes → Drive upload) running silently with no status message sent first. Rule #9 in AGENTS.md explicitly prohibits this. | Root cause: treated multi-step tool chains as a single "do the thing" action without checking the >2 tool / >15s threshold. Rule #9 is non-negotiable: any chain of >2 tool calls OR >15s expected duration → send a one-line status reply FIRST, then execute. No exceptions. |
| 2026-03-11 | 5 crons had consecutiveErrors 1-3 with timeout failures for multiple days. Never flagged or fixed autonomously — JT had to point it out. | Root cause: heartbeat only checked cost alerts and tasks, never scanned cron health. Cron health check is now mandatory at every heartbeat (step 3a in HEARTBEAT.md): scan all jobs for consecutiveErrors >= 2, diagnose, fix autonomously, log what was changed. No cron stays broken past one heartbeat cycle. |
| 2026-03-10 | Overnight agent re-added B2B Account Service Agent card to jtsomwaru.com — MEMORY.md has explicit hard rule: "do not re-add." Second offense. | Root cause: overnight agent reads portfolio-updater/AGENT.md and queue.jsonl but does NOT read MEMORY.md hard rules section. Rule: hard "never do X" decisions must be in the relevant AGENT.md, not just MEMORY.md. |
| 2026-03-09 | Overnight agent re-added B2B Account Service Agent card to jtsomwaru.com after JT explicitly decided against it (2026-03-06). | Root cause: treating MEMORY.md as a sufficient safeguard for isolated agents. Rule: whenever a "never do X again" decision is made, immediately add it to the relevant AGENT.md as a hard constraint, not just MEMORY.md. |
| 2026-03-09 | Chained 3+ sequential exec calls without sending JT any interim reply → appeared frozen → JT had to manually restart gateway twice in one session. | Root cause: treated multi-step tool chains as atomic silent operations. Rule: ALWAYS send a brief status reply before chaining >2 tool calls or any operation expected to take >15s. See Core Rule #9. |
| 2026-03-09 | Same silence pattern continued all morning across deck rebuilds — JT had to check in repeatedly ("fixed?", "are you there?") after every task. Rule already existed, not enforced. | Hard sequence is mandatory: (1) send reply FIRST, (2) do tool work, (3) send result. Step 1 cannot be skipped even if the reply is just "On it, rebuilding now." Silence while JT is watching = failure. |
| 2026-03-08 | Got stuck in a response loop — kept repeating the same output, JT had to manually restart the gateway to stop it. | Root cause unclear: likely Telegram re-delivery loop triggered by a failed edit mid-turn. Rule: if a tool call fails and I'm about to retry the same operation a second time, STOP — write what's done so far, tell JT the specific step that failed, and wait for input. |
| 2026-03-08 | Called restart-gateway.sh mid-turn (during active session) → gateway restarted while response was in-flight → orphaned reply → JT had to manually restart twice. | Root cause: restart script fires SIGUSR1 immediately without waiting for session completion. Rule: NEVER call restart-gateway.sh during an active conversation turn. For in-session restarts, use `gateway action=restart` (handles session completion gracefully). |
| 2026-03-06 | Overnight agent built B2B Account Service Agent and added it to jtsomwaru.com — this agent was decided against, Agentforce work is banned overnight (sync not set up), and nothing unbuilt/untested should touch the site. | Root cause: overnight agent had no hard rule blocking Agentforce builds or unverified site additions. Rule: hard Agentforce overnight ban added. Site additions require fully built + tested + JT-approved. No exceptions. |
| 2026-03-04 | Did not update n8n-agent/tasks/lessons.md after support triage build. | Root cause: close-out checklist was workspace-only; lessons.md is repo-specific and had no explicit rule. Rule: n8n demo close-out checklist — lessons.md is step 3, mandatory, before the task is considered done. |
| 2026-03-03 | Overnight agent flagged compaction.mode=safeguard as a regression, claimed it should be "aggressive" — mode doesn't exist in schema. | Valid modes are only "default" and "safeguard". Safeguard is correct — preserves cache hits. Never change based on this flag again. |
| 2026-03-03 | Promoted job application tasks (Squarespace SA, Writer SA) to HIGH during board review — directly violates locked priority order. | Priority order is locked: (1) demos + site → (2) LinkedIn update → (3) job apps + outreach. Job apps are always MEDIUM until demos and jtsomwaru.com are done. Never promote job app tasks above MEDIUM during a board review without explicit JT override. |
| 2026-03-02 | 16:12 heartbeat logged "Mission Control: unreachable (board may be down)" without attempting kickstart — board stayed down for 6 more hours until 10PM. | TOOLS.md rule is mandatory. Unreachable Mission Control = run kickstart immediately in the same heartbeat, not in a future one. No exceptions. |
| 2026-03-01 | Overnight sub-agent didn't write log to agents/overnight/logs/ — work logged in daily note, morning brief missed overnight section. | Overnight log must be written to correct path agents/overnight/logs/YYYY-MM-DD-log.md. Log file creation is Step 0 before task execution, not Step 6 after. |
| 2026-02-28 | "Build idea:" tasks defaulted to `high` in push template → backlog cluttered the top of the board, displaced actionable work. | Changed template default to `medium`. Added Task Priority Rules section with dependency-aware logic. |
| 2026-02-28 | Cost tracker used API pricing for Anthropic models → phantom $9–10/day alerts that weren't real charges. | Zeroed Anthropic pricing in cost-tracker.py. Recalibrated alert thresholds. Always check auth-profiles.json token prefix before making billing assumptions. |
| 2026-02-25 | Isolated cron no model set → Opus default ($0.57). | Always set model explicitly in isolated agentTurn payloads. |
| 2026-02-25 | compaction.mode: safeguard → context reset on long builds. | Changed to aggressive (this was later reverted — safeguard is correct). |
| 2026-02-25 | Created 6 deleteAfterRun one-shot jobs → past-due scheduler loop → rate limit cooldown → full outage. | NEVER use deleteAfterRun. Rules added above. |
| 2026-02-25 | LaunchAgents for Mission Control + n8n + ngrok → always-on LLM API calls → hammered rate limit. | LaunchAgents for pure automation only. Rules added above. |
| 2026-02-21 | gateway config.patch → dropped JT's connection. | Use restart script always. |
| 2026-02-21 | Arbitrary key in openclaw.json → crashed gateway. | Documented keys only, rest → TOOLS.md only. |

## 2026-03-16 — Silent multi-step tool chains during active conversation
**Failure:** Repeatedly ran multi-step exec chains (NB2 image generation, Drive uploads) without sending interim status replies. JT had to ask "update?" 8+ times in a single conversation.
**Root cause:** Treated Rule 9 (send status reply before chaining tools) as optional when the task felt "nearly done." Skipped the reply step under the assumption the next result would arrive quickly.
**Rule:** Any chain of >2 tool calls OR expected duration >15s = send a brief reply to JT BEFORE starting. Then work. Then send completion. No exceptions, even if the task feels close to done.

## 2026-03-15 (2) — Used raw launchctl to reload LaunchAgent, killing gateway twice
**Failure:** Ran `launchctl unload ~/Library/LaunchAgents/ai.openclaw.gateway.plist` twice during an active session to apply ThrottleInterval change. Gateway went offline both times. JT had to manually restart the second time.
**Root cause:** AGENTS.md rule says "NEVER raw launchctl" but I applied it to a config change I needed to reload — didn't recognize that "never restart via launchctl" includes unload/load cycles. Treated the rule as applying only to restarts, not reloads.
**Rule:** LaunchAgent plist changes that require a reload = warn JT the gateway will briefly go offline before proceeding, then use restart script. Never run `launchctl unload` or `launchctl load` silently mid-session. If the change can be deferred until JT-initiated restart, defer it.

## 2026-03-16 — Repeatedly stopped mid-task without sending status updates
**Failure:** Chained 4+ sequential tool calls in a single session without sending interim replies to JT. JT had to ask "update?" 6+ times to get status.
**Root cause:** Rule 9 exists but I treated it as optional when I was "close to done." Every tool chain felt like it was "almost finished" so I skipped the status reply each time.
**Rule:** No exceptions to Rule 9. Any operation with >2 tool calls OR >15s expected duration = send a brief status reply BEFORE starting. "Almost done" is not an exception. If I'm already mid-chain and realize I forgot — send the update immediately, don't wait until the end.

---

### 2026-03-18 — build_agentforce_t2_deck.py layout collisions

**Slide 5:** `s_s3` ("0") and `s_s3l` (label) shared the same `x` and `y` coordinates — red "0" overlapped "agent escalations" text.
- **Root cause:** Label width was set too wide and started at the same x as the number, treating them as a single element instead of side-by-side.
- **Rule:** Stat + label pairs must use separate x positions. Number gets narrow width (w≤70), label starts at `x + number_width + 6px` gutter.

**Slide 4:** Body text at `size=22` overflowed its `h=165` container, causing "LIVE DEMO ↓" badge at `y=290` to render on top of spilled text.
- **Root cause:** Font size not validated against container height. 22pt × 3 paragraphs ≈ 198px needed, container only 165px.
- **Rule:** Body text in a 165h container → max size=18. Always check: `lines × (size * 1.4) ≤ container_h`. "LIVE DEMO" CTA must sit at `y ≥ 305` to clear body overflow.

## 2026-03-19 — Em dash in outreach DM draft
**Failure:** Generated em dash ("Sara —") in Relation Insurance M1 DM despite explicit rule banning em dashes in all output.
**Root cause:** Did not load cold-email/SKILL.md before drafting. Relied on context/memory instead of reading the skill file. Rule is in the file — not reliably in working memory.
**Rule:** Before drafting ANY outreach message (T1/T2/T3, M1/M2/M3), load cold-email/SKILL.md first. No exceptions. This is the same as the content-voice.md requirement for posts — skill file must be read, not assumed.

| 2026-03-22 | Em dash in Sunday LinkedIn paragraph 3 + contrarian flip pattern in Sunday X post despite explicit bans on both. 5 additional em dashes found in the same week's other posts. | Root cause: both rules were positioned as *post-generation checklist items* ("scan and fix") rather than *generation-time constraints* ("never write"). The model generates naturally using em dashes as stylistic separators and contrarian flips as retrospective framing, then the self-review checklist fails to catch them because they feel contextually correct. "Scan and fix" depends on fallible self-review. "Never generate" is a pre-writing constraint. Fixed: PRE-GENERATION CONSTRAINTS block in both LinkedIn and X crons now explicitly names these as generation-time prohibitions with restructuring instructions, not post-hoc corrections. Checklist Q2/Q3 labels them as constraint failures (rewrite sentence from scratch), not minor fixes. |


## Recently Added (2026-03-24 to 2026-03-25)
| Date | Mistake | Fix |
|------|---------|-----|
| 2026-03-24 | Told JT to "pull latest and redeploy" on Replit after pushing code changes. Replit redeploy reuses the old build — new code didn't appear. Root cause: assumed Replit deploy = rebuild, which is wrong. | Rule: **After any Glow Index code push, instruct JT to do a fresh build, not just redeploy.** Shell: `npm run build` → redeploy. Or Deployments → "Rebuild from scratch." Always say this explicitly. |
| 2026-03-21 | Autoresearch validated 3 cron fixes but created MC task instead of applying them. | Rule: **Validated fix = apply immediately. `cron update` exists. Use it.** |
| 2026-03-19 | LinkedIn DMs drafted with 20-min call CTA and "I built X" in M1 — both explicitly banned in cold-email/SKILL.md. Root cause: drafted from session memory instead of loading the skill file. | Rule: **cold-email/SKILL.md must be loaded and rules checklist run against every draft before output. Memory of skill rules does not substitute for reading the file.** |
| 2026-03-24 | G&G Electric M1 delivered with em dash in body and assertive pain opener — both explicitly banned. Root cause: read the skill file but skipped Step 1 (load good examples + run anti-pattern checklist) and skipped the pre-output checklist entirely before delivering. Reading the skill ≠ following the skill. | Rule: **The pre-output checklist in cold-email/SKILL.md must be run explicitly and shown as ✅/❌ in the reply BEFORE uploading to Drive or delivering the draft. No exceptions.** |
| 2026-03-24 | Resume generated with "Opticfy" company name and missing Spectrum experience. Root cause: resume markdown format didn't match parser expectations, parser returned empty, script fell through to stale hardcoded fallback. | Rule: **After calling build_resume_docx.py, always run parse verification (`parse_resume_md()`) before uploading. Verify: name correct, all experience jobs present, no fallback content.** |
| 2026-03-25 | Cover letter uploaded blank — only header and closing rendered, body missing. Root cause: `parse_cover_letter_md()` requires exactly two `---` separators; cover letter markdown had only one. | Rule: **Cover letter markdown must have two `---` separators. Always run `parse_cover_letter_md()` verification (check `paragraphs` count > 0 and P1 preview) before uploading.** |
| 2026-03-24 | Told JT to redeploy Replit without rebuild — new code didn't appear. | Rule: **After any code push, instruct: Shell → `npm run build` → redeploy. OR Deployments → "Rebuild from scratch."** |

| 2026-03-30 | recent-builds.md missing 7 days of builds (Opus synthesis, product image system, logo generator, Vercel standard, email pivot). Root cause: Proof Points Auto-Update Rule only wrote to content-voice.md, not recent-builds.md. Overnight agent was the only other write path — 24hr lag minimum, and it missed entries during busy nights. Content cron ran Monday morning from stale recent-builds.md. | Rule: **Recent Builds Auto-Update Rule (mandatory): every build completed in-session → append to recent-builds.md same turn. Not "overnight will catch it." Same turn. Rule added to AGENTS.md.** |

| 2026-03-31 | Content cron used "Superior Plumbing, Bronx" as company name in a LinkedIn post. Rule existed in content-voice.md but was not an explicit numbered constraint (C1-C9) in cron payloads — the model read the observations source which named the company and used it literally. Root cause: no-company-names rule existed as prose in content-voice.md but was not enforced as a hard-numbered pre-generation constraint in cron payloads. Fix: added C10 (company name ban) to content-generate-x and content-reminder quality gate. content-generate-linkedin already had C10. Also scrubbed company name from consulting-observations.md source entry and weekly-2026-04-06.md. | Rule: **Company names are BANNED in all content. C10 is now a numbered hard constraint in all 3 content crons. content-reminder quality gate check j scans for company names before send. Source data (consulting-observations.md) must also be anonymized at write time — overnight pipeline must not include company names in observation entries.** |
| 2026-03-31 | Spawned Claude Code via exec background:true in the main session while JT was in an active conversation. Gateway froze, JT had to manually restart. Root cause: Rule 9a says NEVER run claude CLI subprocesses from exec in the main session — the background flag does not prevent the gateway freeze. | Rule: **Claude Code work must use sessions_spawn with runtime="acp" (push-based, non-blocking) when triggered mid-conversation. Never exec claude --print from within an active conversation session regardless of background flag.** |

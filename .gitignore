# ── Databases ─────────────────────────────────────────────
health/health.sqlite
knowledge/kb.sqlite
lcm.db
*.sqlite
*.sqlite-wal
*.sqlite-shm

# ── Secrets / credentials (NEVER commit) ──────────────────
.env
.env.*
!.env.example
config/google-oauth-token.json
config/google-oauth-client.json
config/*oauth*.json
config/*.token.json
config/*.key.json
memory/app-marketing/.ga4-oauth-token-cache.json
*.pem
*.p12

# ── Logs & ephemeral runtime state ────────────────────────
*.log
logs/
interventions.jsonl
memory/costs/*.json
memory/niche-monitor-cost.log

# ── Daily notes (personal health/financial/prospect data) ─
# Pattern: memory/YYYY-MM-DD.md and memory/YYYY-MM-DD-*.md
memory/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9].md
memory/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.md

# ── Agent state & queue files (runtime, high churn) ───────
agents/*/state.json
agents/*/queue.jsonl
agents/portfolio-updater/update-log.md
agents/overnight/logs/

# ── Generated binary assets ───────────────────────────────
agents/vibe-marketing/generated-images/
spanish/audio/
*.png
*.jpg
*.jpeg
*.mp4
*.mov

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# ── Node ──────────────────────────────────────────────────
node_modules/
.next/

# ── OS ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.tmp
*.swp

# ── Backups (stored separately via backup.sh) ─────────────
backups/
memory/passive-income/.trends-cache/
